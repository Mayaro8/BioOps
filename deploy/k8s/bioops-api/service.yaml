apiVersion: v1
kind: Service
metadata:
  name: bioops-api
  namespace: bioops-dev
  labels:
    app: bioops-api
    app.kubernetes.io/part-of: bioops
spec:
  type: ClusterIP

  selector:
    app: bioops-api

  ports:
    - name: http
      port: 8000
      targetPort: http
      protocol: TCP
